name: Test OSS scan workflow

on:
  workflow_dispatch:

permissions: {}

jobs:
  periphery:
    permissions:
      contents: read
      id-token: write
    uses: periphery-pro/actions/.github/workflows/scan.yml@v1
